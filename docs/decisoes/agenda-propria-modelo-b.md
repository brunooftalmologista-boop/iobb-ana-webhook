---
name: agenda-propria-modelo-b
description: "Nova agenda própria (tabela appointments) como fonte única de agendamento — por que, decisões travadas, Fase 1 feita, Fase 2 pendente"
metadata: 
  node_type: memory
  type: project
  originSessionId: 4d16038e-588c-4855-9d79-075f25d2104c
  modified: 2026-07-28T17:18:40.486Z
---

**Página /agenda HÍBRIDA (2026-07-26, commits f15720b + 5f373f1, NO AR e verificado):** página única `agenda.html` servida em `GET /agenda` (e atalho `iobb.com.br/agenda` via `_redirects` no Cloudflare Pages, zip `IOBB_site_ATALHOS.zip` — publicar se ainda não foi). Dupla função: (1) TRANSFERÊNCIA p/ prontuário — agrupada por dia, clique em campo copia, "copiar ficha" copia bloco completo, checkbox "transferido" (localStorage `iobb_transf`) risca a linha; (2) MARCAÇÃO — botão "➕ Agendar" abre modal com vagas reais de `/api/agenda/slots`, grava via `/api/agenda/book` (origem=`secretaria`, criadoPor=email do painel; 409 taken → recarrega vagas), e ✕ desmarca ana/secretaria via `/api/agenda/:id/cancel` (iClinic intocado). Filtro padrão "Só p/ transferir" = `origem != 'iclinic'` (Ana + secretária — CUIDADO: não voltar a filtrar só 'ana', senão o que a secretária marca some da lista de transferência). Auth: MESMAS chaves localStorage do painel (`iobb_at`/`iobb_rt`) + `/api/login` `{agent,password}` + `/api/refresh` — login no painel vale na /agenda (mesma origin onrender). Nascimento viaja em `observacoes` ("Nascimento: X") e é parseado no cliente. Ideia v2 anotada: "transferido" no banco (coluna) p/ sincronizar entre computadores, em vez de localStorage.

**Bloco [CARTEIRINHA] (2026-07-26, commit 242e278, NO AR):** a Ana emite `[CARTEIRINHA] convenio: X | numero: Y [/CARTEIRINHA]` quando lê a foto do cartão (visão restrita do PR #25) ou quando o paciente digita o número. `processarCarteirinhaDaAna` anexa "Carteirinha: <nº> (<convênio>)" às `observacoes` do agendamento ATIVO (busca por conversation_id, fallback paciente_telefone; só origem ana/secretaria; dedupe se já tem "Carteirinha:"), atualiza `convenio` e espelha confirmação à secretária. Sem agendamento ativo (fluxo pré-agendamento) → no-op logado (a equipe já recebe a foto). Extraído ANTES dos outros blocos e processado DEPOIS do [AGENDAR] (mesma msg já encontra a ficha). A /agenda parseia `carteirinha:` das observacoes → campo CARTEIRINHA copiável + linha na ficha. Prompt: seção "### Registro da carteirinha [CARTEIRINHA]" + visão dinâmica atualizada p/ emitir o bloco.

**Agendamento MULTI-PACIENTE (2026-07-27, commit `d11a831`, NO AR):** a Ana travava com "marcar p/ mim e minha mãe" por 3 motivos: prompt "UM horário por vez"; webhook processava só o 1º bloco [AGENDAR]; e o 2º agendamento da conversa era tratado como REMARCAÇÃO → marcar o 2º paciente CANCELAVA o 1º. Corrigido: (a) prompt ganhou seção "Agendamento para MAIS DE UM paciente" (coletar nome+nascimento de cada; ofertar N horários p/ N pacientes preferindo SEQUÊNCIA no mesmo dia, ex. 14h20+14h40; dizer qual é de quem; UM bloco [AGENDAR] POR paciente na MESMA mensagem, nunca o mesmo inicio em dois) + exceção explícita na instrução injetada do horário único; (b) `extrairAgendar` devolve `{limpo, registros: []}` (regex global + fallback sem fechamento) — TODOS os call sites usam `ag.registros`; (c) idempotência/remarcação POR PACIENTE em processarAgendarDaAna: compara `paciente_nome` normalizado (NFD sem acento, lowercase) entre os 5 últimos ativos da conversa — mesmo nome+mesmo inicio=re-emit; mesmo nome+outro inicio=remarcação; nome DIFERENTE=paciente adicional (não cancela); sem nome=comportamento antigo. `agendouOk`=todos gravaram. A trava prosa×token se desativa sozinha com 2+ horários na prosa (exige horário único citado). Limitação conhecida: dois pacientes com o MESMO nome na mesma conversa seriam confundidos (aceito, raro).

**EQUILÍBRIO DE AGENDA — Ana prefere o Conjunto Nacional (2026-07-27, commits `dd4361d`/`71990b7`/`985899d`, NO AR e testado):** ocupação medida em 27/07 mostrou Taguatinga 84-89% e Conjunto 23-36% (e o Conjunto tem MAIS capacidade: 3 dias × 22 = 66 vagas/semana vs 38 da Taguatinga). Causa provável: a campanha que converte (Águas Claras, R$60/dia) alimenta a Taguatinga, enquanto a Asa Norte não converte; e as campanhas de especialidade (refrativa/ceratocone/escleral/catarata), que OBRIGATORIAMENTE atendem no Conjunto (Pentacam + contactóloga só lá), são as mais sufocadas de verba. Alavanca implementada (sem custo): env **`ANA_UNIDADE_PREFERIDA`** (default "Conjunto Nacional"; vazio desliga, "Taguatinga" inverte). Atua em DOIS pontos: (1) prompt dinâmico — só quando `detectUnidade(messages)` é null, a Ana menciona ao perguntar que "há mais disponibilidade no Conjunto Nacional"; (2) **ORDENAÇÃO DA LISTA de vagas** — slots da unidade preferida vêm PRIMEIRO (sort estável, cronologia preservada dentro da unidade). ⚠️ LIÇÃO: pedir no texto NÃO bastou (1º teste: ela só perguntava a unidade; para "mais próximo" ofereceu Taguatinga) — como o prompt manda "escolha UM horário da lista", quem decide na prática é a ORDEM da lista. Travas: preferência explícita do paciente vence na hora; pedido de "horário mais próximo/mais cedo" ganha mesmo sendo da outra unidade; proibido dizer que a outra unidade está cheia. Testado 3 cenários: sem preferência → "No momento temos mais disponibilidade no Conjunto Nacional"; urgência → ofereceu Taguatinga (mais próximo, correto); "quero Águas Claras" → respeitou.

**🔴 BUG RECORRENTE — FILTRO DE UNIDADE ESCONDIA VAGAS (corrigido 2026-07-28, commit `74dcfc9`):** 3ª vez que `detectUnidade` causa "a Ana diz que não tem vaga quando tem". Caso Laiane (28/07 19:36): pediu remarcar p/ SEXTA e escreveu "pode ser em outra unidade **se caso ele não atender no Taguatinga Shopping**" → detectUnidade leu a menção como PREFERÊNCIA → `fetchSlotsDB("taguatinga")` → Ana respondeu "não tenho horários para o Conjunto Nacional nesta sexta" com **~15 vagas livres** lá (sexta 31/07 tinha só 7/22 ocupados). A paciente desistiu e manteve o horário antigo. **FIX ESTRUTURAL: a oferta agora chama SEMPRE `fetchSlotsDB(null)` (as duas unidades).** A unidade detectada passou a servir só para ORDENAR a lista (`unidadePrioritaria` = a citada pelo paciente, senão `ANA_UNIDADE_PREFERIDA`) — NUNCA para filtrar. Regra de ouro para o futuro: **não filtrar slots por unidade detectada; ordenar sim, esconder nunca.** Prompt reforçado: avisa que a lista tem as DUAS unidades, proíbe dizer "não há horário" sem varrer a lista toda, e lembra que pedir um DIA já define a unidade (seg/qua/sex=Conjunto; ter/qui=Taguatinga). Testado com a mensagem literal da Laiane → "Tenho horários disponíveis nesta sexta, 31/07". Corrigido junto: a Ana inventava o dia da semana ("quinta-feira, 31/07" p/ uma sexta) — agora deve copiar o dia da semana exatamente da linha da lista.

**Bugs 27/07 corrigidos:** (1) **Guerra de refresh token painel×/agenda** (commit `b8b706b`): as duas páginas compartilham iobb_at/iobb_rt mas guardavam tokens EM MEMÓRIA; refresh token Supabase é USO ÚNICO → aba com token velho renovava e revogava a família toda → caixas do painel "aparecendo/sumindo", logout em loop. Fix nas duas páginas: `syncTokensFromStorage()` antes de cada request/refresh (adota sessão mais nova da outra aba) + trava anti-corrida `iobb_rt_ts` (janela 8s, espera 1,5s e adota). Sessões antigas foram revogadas — precisou relogar 1x. (2) **"Semana que vem" errado** (commit `5c73d3e`): paciente pediu "semana que vem", Ana ofereceu qua 29/07 (MESMA semana) ecoando a expressão. Âncora de data do dynamicPrompt agora traz "ESTA SEMANA até domingo DD/MM" + "SEMANA QUE VEM começa segunda DD/MM" (getUTCDay sobre ymd; testado seg/sex/sab/dom) + regra: nunca rotular data desta semana como "semana que vem"; pedido de próxima semana → ofertar a partir da próxima segunda.

**🔴 REGRA VIGENTE DE ANTECEDÊNCIA (2026-07-28, commit `361b3a9`) — SUBSTITUI a regra descrita no parágrafo seguinte:** o Dr. Bruno inverteu o padrão. **Mesmo dia liberado para particular E para TODOS os convênios credenciados.** Só 5 planos exigem ~24h (`ANA_ANTECEDENCIA_HORAS`), porque a liberação junto à operadora não sai em cima da hora: **Unimed (todas as variações), Casec, Codevasc, Care Plus, Life Empresarial** — constante `CONVENIOS_COM_ANTECEDENCIA` + funções `exigeAntecedencia(texto)` e `conversaExigeAntecedencia(messages)` (acento/caixa/hífen-insensíveis; 26 casos testados). Aplicada em 4 pontos: oferta (padrão LIBERADO; filtra só se o paciente citar um dos 5) e os 3 pontos de gravação em processarAgendarDaAna (agora sobre o campo `convenio`, não mais `/particular/`). A marcação interna `[SÓ PARTICULAR]` foi APOSENTADA (`tagAte = 0`) — era o que fazia a Ana dizer "hoje só atendemos particular" (vetado pelo Bruno, ver [[regras-atendimento-jul2026]] item 5). ✅ RESOLVIDO (commit `45053cf`): o "Codevasc" da regra era a **CODEVASF** — e **CASEC é o plano de saúde dos funcionários da CODEVASF** (mesmo convênio, dois nomes, confirmado pelo Bruno). Grafias casec/codevasf/codevasc cobertas na constante; equivalência declarada na seção NOMES COMPOSTOS; lista de credenciados agora exibe "CASEC (CODEVASF)". Testado: "sou funcionário da Codevasf" → "atendemos o seu plano — a CASEC…"; "meu convênio é Casec" → ofertou respeitando as 24h. Testado: TJDFT recebeu horário de amanhã (que a regra antiga esconderia); Unimed pulou a janela e ainda pediu a carteirinha.

**Buffer de antecedência (2026-07-20, commit ec218e7; CONDICIONAL desde 2026-07-23 commit ac61adb) — HISTÓRICO, superado pela regra acima:** a Ana filtra horários por antecedência. AGORA depende do tipo de atendimento: **PARTICULAR → buffer 0 (pode agendar no MESMO dia)**; **CONVÊNIO → `ANA_ANTECEDENCIA_HORAS` (24h)**, e se o paciente quiser antes cai no pré-agendamento (equipe). ⚠️ CORREÇÃO 2026-07-27 (commit `3de5442`): o buffer na OFERTA escondia as vagas de hoje de quem AINDA NÃO tinha dito "particular" → Ana negava "hoje não tem" a paciente particular (caso Sebastião 27/07: 15:40 livre no Conjunto e negado; ele acabou marcado p/ 28/07). Agora a lista injetada traz TODOS os slots >= agora; os dentro da janela do convênio vêm com sufixo ` [SÓ PARTICULAR]` (após o token [inicio:], via 3º parâmetro `tagParticularAteTs` de formatSlotsParaAgendar) + instrução: perguntar "particular ou convênio?" antes de negar horário próximo; marcados só p/ particular confirmado; convênio → primeiro sem marca ou [PREAGENDAMENTO] p/ encaixe. Rede de segurança inalterada: validação na gravação continua barrando convênio na janela. Detecção: `detectAtendimentoParticular(messages)` na OFERTA (só true se o paciente disse "particular" e não citou convênio/plano/unimed); na MARCAÇÃO/colisão usa o campo `convenio` do bloco [AGENDAR] (regex /particular/). Aplicado nos 3 pontos (injeção ~2517, validação do inicio ~1035, colisão ~1053). Vale SÓ para a Ana; painel/secretária não filtra. `ANA_ANTECEDENCIA_HORAS` (env) segue valendo p/ convênio (padrão 24h). NOTA: também há a VALIDAÇÃO do inicio na marcação (processarAgendarDaAna ~1029) que rejeita horário fora da lista vigente (dia/unidade/grade/buffer) — implementada; os horários 12:40 E 17:40 são bloqueados na grade em todas as unidades (getAvailableSlots/agendaPorDia; commits 7e13317 e 8bdf4c9) — via `if ((h===12||h===17) && m===40) continue`.

**Import iClinic (2026-07-20):** lançados 70 agendamentos da semana 19-25/07 (origem 'iclinic', criado_por 'import-iclinic') p/ bloquear os slots — Conjunto 45 (seg 20/qua 22/sex 24), Taguatinga 25 (ter 21/qui 23). Fonte: iClinic ao vivo no Chrome do Bruno (get_page_text da Agenda, unidades IOBB Conjunto Nacional e IOBB Taguatinga Shopping; PDF export tem fonte embutida com encoding próprio, ilegível sem lib). PENDENTE: **próxima semana (26/07+) NÃO bloqueada** — a Ana enxerga 14 dias, então há risco de overbooking lá; refazer o mesmo processo p/ as próximas semanas. DESCOBERTA: **Taguatinga atende em PARALELO** (2-3 cadeiras no mesmo horário) — o modelo fila-única não representa isso; decisão do Bruno = bloquear por horário (Ana sub-oferece Taguatinga mas nunca overbooka). Futuro: modelar capacidade/cadeiras da Taguatinga.

**Revisão do SYSTEM_PROMPT (2026-07-20, commit 14a0da6):** prompt da Ana auditado (2 revisores independentes + re-verificação) e corrigido p/ coerência da Fase 2 — identidade/fluxo/coleta/[PREAGENDAMENTO] reescritos ([AGENDAR] é o principal com agenda; pré-agendamento é fallback explícito, precedência clara). Corrigidas contradições internas (✅/emoji no exemplo, "tomografia" vs regra do OCT, pedir telefone vs "já conhecido") e preenchidas lacunas (segunda via receita, retorno, recibo, ótica, teleconsulta presencial, horário manhã/tarde).

**BUG "Ana só oferece Taguatinga / diz que Conjunto não tem vaga" (2026-07-22, commit 0a86704).** Bruno reportou a Ana "se perdendo": não achava horário do Conjunto mesmo com 21+ vagas livres. Causa: `detectUnidade` lia as mensagens da PRÓPRIA Ana além das do paciente; como a Ana, ao perguntar a preferência, cita as DUAS unidades ("Conjunto Nacional (Asa Norte) ou Taguatinga Shopping (Águas Claras)?") e "taguatinga/aguas claras" era checado primeiro, a função retornava Taguatinga em quase toda conversa → `fetchSlotsDB("taguatinga")` → Ana recebia só vagas da Taguatinga e dizia que o Conjunto estava sem vaga. Fix: `detectUnidade` agora filtra role="user" (só o paciente), ignora mensagens-menu que citam as duas, e pega a última unidade mencionada pelo paciente (teste 5/5). NÃO era o RLS (descartado: migration do lockdown foi 21/07 23:00 e o sync gravou 74 linhas em 22/07 10:16 sem erro — servidor usa service_role, imune a RLS). Resolvido (separado, commit e659640): a Ana não sabia informar "quanto tempo sem lente de contato antes da consulta/topografia" — adicionada no prompt a orientação do Dr. Bruno: gelatinosa suspende 24h antes, rígida/escleral 48h antes.

**DESMARCAR/REMARCAR pela Ana (2026-07-25, commit a7e8639) — testado e aprovado pelo Bruno.** Bloco `[CANCELAR] inicio|unidade`; `processarCancelarDaAna` SÓ cancela agendamentos origem='ana' do próprio telefone; iClinic/secretária → encaminha à equipe (não divergir); nada ativo → não alarma (remarcação já tratada). Remarcar = [CANCELAR] antigo + [AGENDAR] novo na MESMA msg → marca o novo primeiro, só cancela o antigo se o novo gravou (paciente nunca fica sem horário). agendamentosDoPaciente injeta os agendamentos com token [inicio:] e marca quais a Ana pode mexer (origem ana) vs "só pela equipe". Prompt: confirmar antes de cancelar.

**Batch de fixes de agendamento (2026-07-24/25):** (a) OVERBOOKING por remarcação bloqueada (commit fad4db8): a guarda de idempotência bloqueava QUALQUER 2º [AGENDAR] → quando o paciente remarcava, a Ana confirmava mas não salvava (ficava com o antigo, novo virava slot livre p/ outro). Agora: mesmo horário=re-emit ignorado; horário diferente=remarcação (cria novo, cancela antigo). (b) Ana ALUCINAVA o horário ao repetir a confirmação (commit 593c20a): "Agendado 11h40" quando salvou 11h20 → prompt: NÃO repetir data/hora depois de confirmar; ao receber carteirinha depois, só agradecer. (c) Ana dizia "só tem de manhã" com a tarde toda livre (commit ba23f7c): formatSlotsParaAgendar tinha teto GLOBAL de 12 slots → a tarde dos dias à frente sumia; agora agrupa por dia e mostra todos os horários de cada dia (até 8 dias). (d) CARTEIRINHA: ao fechar agendamento de convênio, a Ana pede a carteirinha (foto/número) p/ anexar (commit e80ad46); detecção da foto da carteirinha robusta (últimas 3 msgs da Ana + contexto de convênio) e regra p/ não reperguntar particular/convênio (commit aabdaef). (e) "UM horário por vez" reforçado — proibido enumerar mesmo se o paciente pedir "todos" (commit 6e4a2d9). PENDENTE observado: a Ana às vezes responde POR CIMA da secretária mesmo após ela assumir (visto na conversa da Carla) — investigar concorrência status human.

**Dois fixes (2026-07-23, commit ab13235):** (1) a Ana ERRAVA a unidade do dia (deduzia o dia da semana e escorregava) — agora `unidadeDoDia(date)` calcula em código e injeta "HOJE é ... — unidade X" no dynamicPrompt (seg/qua/sex=Conjunto; ter/qui=Taguatinga; fds=sem atendimento). (2) a Ana dizia "não tenho acesso aos agendamentos" — a agenda só era usada p/ VAGAS LIVRES; agora `agendamentosDoPaciente(from)` injeta os agendamentos ativos futuros daquele telefone ("### Agendamentos que ESTE paciente já tem") e a Ana informa (alterações continuam com a equipe). LIMITAÇÃO: casa por paciente_telefone = `from` (wa_id) — pega os que a Ana marcou; agendamentos do iClinic/secretária podem ter telefone em outro formato (ou nome sem telefone) e não aparecem p/ o paciente. Ambos validados por teste sintético isolado (conversa sempre-ativa via ad_click meta/, POST no webhook, resposta lida do banco, dados apagados).

**SYNC iCLINIC→PAINEL LIGADO (2026-07-21): fonte única finalmente resolvida.** O Bruno colou os dois endereços secretos iCal (Google Calendars privados alimentados pela integração nativa do iClinic, COM nomes de paciente): Conjunto `73eadfc2…/private-0b82bcc5…/basic.ics`, Taguatinga `20e341d0…/private-a5f10cea…/basic.ics`. Guardados na tabela `settings` (keys `ical_iclinic_cn` / `ical_iclinic_tg`) — são SEGREDO/LGPD, nunca no código nem público. O sync (`syncCalendarioIClinic`/`syncIClinicTodas`/`startSyncIClinic`, roda no boot + a cada 15 min) reconcilia origem='iclinic', criado_por='sync-iclinic'. Verificado no banco após 1º ciclo: cobertura completa dos 14 dias da janela da Ana nas DUAS unidades (esta semana E as próximas, 27/07→04/08) → resolve o buraco de overbooking da "próxima semana não bloqueada". Removidas as 26 linhas antigas 'import-iclinic' (agora redundantes). Nota: o iCal do Conjunto traz um evento recorrente de 2027 (5 slots far-future) — inofensivo, a Ana só enxerga 14 dias. Com o painel virando fonte única de verdade, a auto-marcação está religada. CONFIRMADO NO AR (2026-07-21): a env `ANA_MARCA_SOZINHA` NÃO existe no Render (Bruno conferiu) → o padrão do código (`readEnv(...) !== "0"`) = LIGADO. A Ana marcou um paciente real de verdade (Pedro Henrique, Conjunto 24/07 12:00). [[ana-webhook-config-deps]]

**BUG do agendamento DUPLO corrigido (2026-07-21, commit 76988b0):** a Ana marcou o Pedro em DOIS horários seguidos (12:00 confirmado + 12:20, este já cancelado). Causa: o modelo RE-EMITE o bloco técnico `[AGENDAR]` num turno seguinte (no caso, quando o paciente comentou que não tinha a receita) e `processarAgendarDaAna` gravava de novo, sem checar duplicidade. Fix: guarda de IDEMPOTÊNCIA POR CONVERSA — antes de gravar, consulta se a conversa já tem agendamento ativo da Ana (origem='ana', status reservado/confirmado); se tiver, ignora o novo [AGENDAR] e retorna {ok:true, already:true}. Reagendamento na mesma conversa fica com a equipe (coerente com o prompt).

**REVERSÃO (2026-07-21, commit 572f500): a Ana voltou ao PRÉ-AGENDAMENTO por decisão do Bruno.** A Fase 2 (marcar sozinha) ficou atrás da env `ANA_MARCA_SOZINHA` (padrão OFF → nenhuma vaga é injetada, Ana coleta preferência + equipe confirma). Motivo honesto: o painel NÃO é a fonte única ainda — secretárias seguem no iClinic e o sync iClinic→painel não foi ligado (Bruno recusou o endereço secreto do iCal), então a agenda do painel diverge do iClinic → auto-marcação = risco de overbooking. RELIGAR o automático (`ANA_MARCA_SOZINHA=1`) só quando o painel virar fonte única (secretárias marcando nele OU sync do iClinic ligado). Bug corrigido no caminho (commit 6416709): a injeção de vagas não disparava quando o paciente citava só a unidade ("Conjunto Nacional") — detectSchedulingIntent/detectUnidade agora reconhecem nomes de unidade e "Águas Claras". O backend de vagas está correto (diagnóstico mostrou 110 vagas). Fonte de sync viável mapeada: Google Calendars privados "Agenda iClinic - …" (73eadfc2… Conjunto, 20e341d0… Taguatinga) alimentados ao vivo pela integração do iClinic, COM nomes — leríamos por iCal secreto ou API.

**STATUS: NO AR (deployado em 2026-07-20).** Fase 1 + Fase 2 mergeadas na main (commit d54c2b8) e deployadas no Render (verificado: /api/agenda/slots → 401 = código novo). Migração aplicada no Supabase projeto `pbnphvmzqdgnijxngosc` (iobb-atendimento): tabela `appointments` (16 colunas) + índice único parcial `appointments_slot_unico` ativo. Bruno assumiu bloquear manualmente no painel os horários já ocupados no iClinic (a data de corte). A branch feat/agenda-propria foi mergeada por fast-forward.

Decisão (2026-07-20): construir uma **agenda própria no Supabase** como FONTE ÚNICA de agendamento, substituindo a leitura do iCal público.

**Por quê:** o iCal `public/basic.ics` ([index.js](index.js) ~74) é só-leitura, atrasado e sem trava → a Ana nunca conseguiu marcar de fato, só propor, e havia risco de overbooking. Por isso a oferta de horários da Ana já estava DESATIVADA (bloco comentado em index.js ~1902). A clínica marca no **iClinic**, mas o iClinic NÃO expõe API REST self-service (docs.iclinic.com.br é só import CSV) — integração só via parceria comercial. Então a saída foi agenda própria.

**Decisões travadas pelo Bruno:**
- **Modelo B (agenda mestre):** a agenda nova é a oficial dessas unidades; secretárias marcam nela, iClinic fica só p/ prontuário.
- **Granularidade só por unidade** (Conjunto Nacional / Taguatinga) — sem separar por profissional.

**Fase 1 — FEITA e COMMITADA (branch `feat/agenda-propria`, nascida da main; ainda não deu push nem PR). 4 commits: 6c7f7f6 (agenda base), 4ff87ff (menu convênio), b0e3a23 (Motivo menu + coluna observacoes), cb20453 (Motivo multi-seleção + "Retorno"):**
- `sql/agenda.sql`: tabela `appointments` (com colunas motivo e observacoes) + índice único parcial `(unidade, inicio) where status in (reservado,confirmado)` = trava anti-overbooking do Postgres (erro 23505). **Bruno precisa rodar essa migração no SQL Editor do Supabase** (como as outras — ver [[ana-webhook-config-deps]]).
- index.js: helpers `fetchBusyFromDB`, `fetchSlotsDB` (reaproveitam getAvailableSlots), `criarAgendamento`, `confirmarAgendamento`, `cancelarAgendamento`, `listarAgendamentos`; endpoints `GET/POST /api/agenda/slots|appointments|book|:id/cancel|:id/move` (atrás do requirePanelAuth).
- painel.html: botão 🗓️ no header → modal Agenda (grade dia/unidade, livres em verde clicáveis, ocupados com cancelar) + modal "Marcar horário" com: Convênio (menu, lista de convênios atendidos), Motivo (CHECKBOXES multi-seleção — Consulta/Retorno/Avaliação de cirurgia + grupo Exames; gravados juntos no campo texto motivo, ex.: "Consulta, Topografia"), Observações (texto livre). Validado visualmente com mock.
- Demo interativa do painel publicada como Artifact (URL fixa https://claude.ai/code/artifact/42bb89b8-9d5a-4382-b593-7dee55368926) — dados fictícios, para o Bruno testar a UI sem backend.

**Fase 2 — FEITA e COMMITADA (2026-07-20, branch `feat/agenda-propria` rebaseada sobre a main atual; commit 33d5058; NÃO deployada).** A Ana oferece e marca horários reais: webhook injeta vagas via `fetchSlotsDB` (cada uma com token `[inicio:...]`); SYSTEM_PROMPT "Como lidar com horários" reescrito (oferece 1 por vez → paciente confirma → bloco técnico `[AGENDAR]`); `extrairAgendar()` faz o parse (preserva o ISO), `processarAgendarDaAna()` grava via criarAgendamento(origem "ana") + marcarConversaoAgendada (Ads) + espelha à secretária. **Marca só ao confirmar (v1, decisão do Bruno):** em corrida (trava do banco → taken) manda correção com a próxima vaga. Degradação: banco fora/sem vaga → cai no `[PREAGENDAMENTO]`. Prioridade no webhook: `[AGENDAR]` > `[PREAGENDAMENTO]` > `[RECADO]`. Parser testado (14/14, scratchpad/test-agendar.js). NÃO roda servidor local (sem secrets) — teste real só no deploy.
Possível endurecimento futuro (não feito): validar o `inicio` do `[AGENDAR]` contra as vagas atuais antes de gravar (hoje confia na Ana copiar o token); e holds (reserva ao oferecer) — descartado na v1 por complexidade.

**Regra de MOTIVO na Fase 2 (decidida com o Bruno em 2026-07-20):** os checkboxes de motivo são SÓ do painel humano — a Ana NÃO usa esse formulário (ela grava direto o texto `motivo` via criarAgendamento). Para a Ana não se complicar: (1) padrão "Consulta" na dúvida; (2) só especializa quando o paciente for claro ("meu retorno" → "Retorno"; "avaliação da cirurgia" → "Avaliação de cirurgia"); (3) a Ana NUNCA enumera/oferece a lista de exames ao paciente; (4) motivo é OPCIONAL e não afeta a marcação (não trava vaga). A lista definida serve de vocabulário-alvo p/ classificação, não de menu a recitar.

**ATENÇÃO ao deployar a Fase 1:** a `feat/agenda-propria` nasceu de uma `main` que estava DESATUALIZADA. Em 2026-07-20 a `main` remota avançou (PRs #20–#23: preços/SIS Senado + correção do horário da equipe, commit abdd402, já em produção). Antes de mergear/deployar a agenda, **rebasear `feat/agenda-propria` sobre a `main` atual**. Deve ser limpo — a agenda mexe em regiões do index.js (~helpers após fetchSlots, endpoints antes de /api/diag/agenda) e no painel.html que os preços NÃO tocaram.

**Ponto operacional do Bruno antes do go-live:** a agenda nova nasce vazia (tudo livre), mas o iClinic tem compromissos futuros. Definir data de corte OU lançar os compromissos já existentes, senão a Ana/secretária oferece horário que no iClinic já está ocupado.
